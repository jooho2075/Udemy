<%- include('partials/head') %>
<body>
    <%- include('partials/navbar') %>
    <h1>Your random number is : <%= num %></h1>
    <!--템플릿의 나머지 부분을 통제할 수 있도록 로직을 추가하는 역할-->
    <!--356.EJS 조건문-->
    <% if(num % 2 === 0) { %>
        <h2>That is an even number</h2>
    <% } else {%>
        <h2>That is an odd number</h2>
    <% }%>

    <h3><%= num % 2 === 0 ? 'even' : 'odd' %></h3>
</body>